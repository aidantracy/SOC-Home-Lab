# pcaps

Raw `.pcap`/`.pcapng` files are gitignored by default (they can be large and may
contain sensitive data). Commit only small, sanitized, intentionally-shared
captures — and describe their source here.
